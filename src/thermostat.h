#ifndef THERMOSTAT_H
#define THERMOSTAT_H

// Function to calculate temperature based on user input
float calculate_temperature(float user_input);

float calculate_temperature_with_offset(float user_input, float offset);

#endif /* THERMOSTAT_H */
